JS KeyFrame
A JavaScript framework for keyboard interactions in web applications.

Dependencies:
jQuery

Running the tests
Please run the JS Test Driver tests before committing any changes to this project

To run the tests, first execute
./test.sh (to start the JS Test Driver server)

Then:
./run-tests.sh (to run the tests)

Currently the tests are set up for running against Firefox on an OSX box. You may need to change the Firefox path to get it to run on your system.